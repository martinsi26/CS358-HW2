
class MyClass {
    public void main() {
	switch (x) {
	}
	switch (x) {
	case 1:
	    break;
	case 2: case 3: case 4: case 5:
	    a = 4;
	    b = 6;
	    break;
	case 4:
	    a = 4;
	    b = 6;
	    break;
	}
	switch (x) {
	default: case 4:
	    x = y;
	    break;
	}
	int x = 4;
	boolean y = 4;
	String s = "abc";
	int[] z = null;
	boolean[][] zz = null;
	String[][][] r = null;
	while (a > 0) a = a + x;
	while (b > 0) {
	}
	while (c > 0) {
	    a = b;
	}
	while (c > 0) {
	    c = e;
	    d = f;
	}
	for (;;) x = y;
	for (a = 5;;) x = y;
	for (; b < 2;) x = y;
	for (int a = 5; b < 2;) x = y;
	for (;;x++) { x = y; }
	for (f(x);;x++) { x = y; }
	for (;g(y);x++) { x = y; z = w; }
	for (String s = t;g(y);x++) { x = y; z = w; }
	if (a < 0) x = x + 1;
	if (a < 0) {
	    x = y;
	    y = z;
	}
	if (a < 0) x = y;
	else a = b;
	if (a < 0) { x = y; a = c;}
	else a = b;
	if (a < 0) x = y;
	else { a = b; j = k; }
	if (a < 0) { x = y; b = 5; }
	else { a = b; c = b; }
	{
	}
	{
	    a = b;
	    b = c;
	}
    }
}
